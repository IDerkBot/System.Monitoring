<UserControl x:Class="SystemMonitoringNetCore.Views.UserControls.SensorEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SystemMonitoringNetCore.ViewModels"
             Width="350"
             HorizontalAlignment="Center"
             VerticalAlignment="Center"
             d:DataContext="{d:DesignInstance vm:SensorEditControlViewModel}"
             mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <Label Content="Идентификатор" />
            <TextBox Height="40" Text="{Binding Sensor.Uid}" />
            <Label Margin="0,10,0,0" Content="Широта" />
            <TextBox Height="40" Text="{Binding Sensor.PositionX}" />
            <Label Margin="0,10,0,0" Content="Долгота" />
            <TextBox Height="40" Text="{Binding Sensor.PositionY}" />
            <Label Margin="0,10,0,0" Content="Поле" />
            <ComboBox Height="40"
                      Margin="0,0,0,20"
                      DisplayMemberPath="Number"
                      ItemsSource="{Binding Fields}"
                      SelectedItem="{Binding Sensor.Field}" />
            <Button Command="{Binding SaveCommand}" Content="Сохранить" />
        </StackPanel>
    </Grid>
</UserControl>
